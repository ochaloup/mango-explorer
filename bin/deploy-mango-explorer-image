#!/bin/bash

set -ueo pipefail

cd /srv/app/marinade-anchor
git pull
docker build --pull --target builder -t marinade-anchor-builder .

cd /srv/app/mango-explorer
git pull
docker pull jupyter/scipy-notebook:latest
docker build -t mango-explorer .
